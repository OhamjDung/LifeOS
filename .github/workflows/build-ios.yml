name: Build iOS IPA

on:
  push:
    branches: [main, master]
    paths:
      - 'mobile/**'
      - '.github/workflows/build-ios.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode 26.2
        run: |
          sudo xcode-select -s /Applications/Xcode_26.2.app/Contents/Developer
          xcodebuild -version
          swift --version

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: mobile/package-lock.json

      - name: Install dependencies
        working-directory: mobile
        run: npm install --legacy-peer-deps

      - name: Debug npm install result
        working-directory: mobile
        run: |
          echo "=== react-native version installed ==="
          node -e "console.log(require('./node_modules/react-native/package.json').version)"
          echo "=== reanimated version ==="
          node -e "console.log(require('./node_modules/react-native-reanimated/package.json').version)" 2>/dev/null || echo "not found"
          echo "=== worklets version ==="
          node -e "console.log(require('./node_modules/react-native-worklets/package.json').version)" 2>/dev/null || echo "not found"
          echo "=== voice package ==="
          node -e "console.log(require('./node_modules/@react-native-voice/voice/package.json').version)" 2>/dev/null || echo "not found"
          echo "=== ExpoModulesCore xcframework in node_modules ==="
          find node_modules/expo-modules-core -name "ExpoModulesCore.xcframework" -maxdepth 5 2>/dev/null || echo "xcframework NOT in node_modules"
          find node_modules/expo-modules-core/ios -maxdepth 2 2>/dev/null | head -20

      - name: Write .env file
        working-directory: mobile
        env:
          EXPO_PUBLIC_SUPABASE_URL: ${{ secrets.EXPO_PUBLIC_SUPABASE_URL }}
          EXPO_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY }}
        run: |
          printf 'EXPO_PUBLIC_SUPABASE_URL=%s\n' "$EXPO_PUBLIC_SUPABASE_URL" > .env
          printf 'EXPO_PUBLIC_SUPABASE_ANON_KEY=%s\n' "$EXPO_PUBLIC_SUPABASE_ANON_KEY" >> .env
          printf 'EXPO_PUBLIC_BUILD_ID=ci-%s\n' "$(date +%Y%m%d-%H%M%S)" >> .env
          echo "=== env var lengths ==="
          echo "URL chars: ${#EXPO_PUBLIC_SUPABASE_URL}"
          echo "KEY chars: ${#EXPO_PUBLIC_SUPABASE_ANON_KEY}"
          echo "=== .env contents (keys only) ==="
          cut -d= -f1 .env

      - name: Expo prebuild
        working-directory: mobile
        run: npx expo prebuild --platform ios --clean --no-install
        env:
          EXPO_PUBLIC_SUPABASE_URL: ${{ secrets.EXPO_PUBLIC_SUPABASE_URL }}
          EXPO_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY }}

      - name: Debug Podfile content
        working-directory: mobile/ios
        run: |
          echo "=== Full Podfile ==="
          cat Podfile
          echo "=== END Podfile ==="

      - name: Patch Podfile for CI
        working-directory: mobile/ios
        run: |
          # Inject ENABLE_USER_SCRIPT_SANDBOXING=NO into existing post_install block
          python3 << 'PYEOF'
          with open('Podfile', 'r') as f:
              content = f.read()
          inject = (
              "\n  installer.pods_project.targets.each do |target|\n"
              "    target.build_configurations.each do |config|\n"
              "      config.build_settings['ENABLE_USER_SCRIPT_SANDBOXING'] = 'NO'\n"
              "    end\n"
              "  end\n"
          )
          content = content.replace('post_install do |installer|', 'post_install do |installer|' + inject, 1)
          with open('Podfile', 'w') as f:
              f.write(content)
          print('Podfile patched successfully')
          PYEOF
          echo "=== Podfile after patch (post_install section) ==="
          awk '/post_install/,/^end/' Podfile | head -30

      - name: Install CocoaPods
        working-directory: mobile/ios
        run: pod install

      - name: Patch EXAV xcconfig for React headers
        working-directory: mobile/ios
        run: |
          echo "=== Locating React headers ==="
          RCT=$(find Pods/Headers/Public -name "RCTBridgeModule.h" 2>/dev/null | head -1)
          echo "RCTBridgeModule.h: ${RCT:-NOT FOUND}"
          REACT_POD=$(echo "$RCT" | awk -F/ 'NF>=4{print $4}')
          export REACT_HDR_PATH="\$(PODS_ROOT)/Headers/Public/${REACT_POD:-React-Core}"
          echo "REACT_HDR_PATH: $REACT_HDR_PATH"

          echo "=== Locating ExpoModulesCore headers (xcframework or source) ==="
          # xcframework: EXEventEmitter.h is inside framework bundle under Pods/
          EXE_XCF=$(find Pods -name "EXEventEmitter.h" 2>/dev/null | grep -v simulator | head -1)
          EXE_HDR=$(find Pods/Headers/Public -name "EXEventEmitter.h" 2>/dev/null | head -1)
          echo "EXEventEmitter.h (xcframework): ${EXE_XCF:-NOT FOUND}"
          echo "EXEventEmitter.h (source):      ${EXE_HDR:-NOT FOUND}"

          if [ -n "$EXE_XCF" ]; then
            # Path: Pods/.../ExpoModulesCore.xcframework/ios-arm64/ExpoModulesCore.framework/Headers/EXEventEmitter.h
            # Framework container = dirname(dirname(dirname(EXE_XCF))) = dir containing ExpoModulesCore.framework
            FW_CONTAINER=$(python3 -c "import os,sys; p='$EXE_XCF'; print(os.path.dirname(os.path.dirname(os.path.dirname(p))))")
            # Convert to $(PODS_ROOT)-relative: strip leading "Pods/"
            FW_REL=$(echo "$FW_CONTAINER" | sed 's|^Pods/||')
            export EXMOD_FW_PATH="\$(PODS_ROOT)/${FW_REL}"
            echo "ExpoModulesCore framework container: $EXMOD_FW_PATH"
          elif [ -n "$EXE_HDR" ]; then
            EXMOD_POD=$(echo "$EXE_HDR" | awk -F/ 'NF>=4{print $4}')
            export EXMOD_FW_PATH=""
            export EXMOD_HDR_PATH="\$(PODS_ROOT)/Headers/Public/${EXMOD_POD:-ExpoModulesCore}"
            echo "ExpoModulesCore source headers: $EXMOD_HDR_PATH"
          else
            export EXMOD_FW_PATH=""
            echo "WARNING: EXEventEmitter.h not found anywhere"
          fi

          echo "=== EXAV xcconfig before patch ==="
          cat "Pods/Target Support Files/EXAV/EXAV.release.xcconfig" 2>/dev/null | grep -E 'SEARCH_PATHS' || echo "(no SEARCH_PATHS lines)"

          python3 << 'PYEOF'
          import os

          react_hdr   = os.environ.get('REACT_HDR_PATH', '$(PODS_ROOT)/Headers/Public/React-Core')
          exmod_fw    = os.environ.get('EXMOD_FW_PATH', '')
          exmod_hdr   = os.environ.get('EXMOD_HDR_PATH', '')

          extra_hdr = f' {react_hdr}'
          if exmod_hdr:
              extra_hdr += f' {exmod_hdr}'

          for cfg in ['debug', 'release']:
              path = f'Pods/Target Support Files/EXAV/EXAV.{cfg}.xcconfig'
              if not os.path.exists(path):
                  print(f'WARNING: {path} not found')
                  continue
              with open(path) as f:
                  lines = f.readlines()

              patched = []
              found_hdr = False
              found_fw  = False
              for l in lines:
                  if l.startswith('HEADER_SEARCH_PATHS') and not found_hdr:
                      patched.append(l.rstrip() + extra_hdr + '\n')
                      found_hdr = True
                  elif l.startswith('FRAMEWORK_SEARCH_PATHS') and not found_fw and exmod_fw:
                      patched.append(l.rstrip() + f' {exmod_fw}' + '\n')
                      found_fw = True
                  else:
                      patched.append(l)

              if not found_hdr:
                  patched.append(f'HEADER_SEARCH_PATHS = $(inherited){extra_hdr}\n')
              if not found_fw and exmod_fw:
                  patched.append(f'FRAMEWORK_SEARCH_PATHS = $(inherited) {exmod_fw}\n')

              with open(path, 'w') as f:
                  f.writelines(patched)

              print(f'--- EXAV.{cfg}.xcconfig SEARCH_PATHS after ---')
              for l in patched:
                  if 'SEARCH_PATHS' in l:
                      print(l.rstrip())
          PYEOF

      - name: Debug post-pod-install
        working-directory: mobile/ios
        run: |
          echo "=== Xcode/Swift version ==="
          xcodebuild -version && swift --version
          echo "=== ExpoModulesCore pod directory ==="
          ls Pods/ExpoModulesCore/ 2>/dev/null | head -20 || echo "dir not found"
          echo "=== ExpoModulesCore xcframework exists? ==="
          ls Pods/ExpoModulesCore/ExpoModulesCore.xcframework 2>/dev/null && echo "YES - xcframework present" || echo "NO - xcframework absent (will build from source)"
          echo "=== ExpoModulesCore xcframework Swift version (if present) ==="
          head -5 "Pods/ExpoModulesCore/ExpoModulesCore.xcframework/ios-arm64/ExpoModulesCore.framework/Modules/ExpoModulesCore.swiftmodule/arm64-apple-ios.swiftinterface" 2>/dev/null || \
          head -5 "Pods/ExpoModulesCore/ExpoModulesCore.xcframework/ios-arm64_x86_64-simulator/ExpoModulesCore.framework/Modules/ExpoModulesCore.swiftmodule/arm64-apple-ios-simulator.swiftinterface" 2>/dev/null || echo "no swiftinterface found"
          echo "=== ExpoModulesJSI pod directory ==="
          ls Pods/ExpoModulesJSI/ 2>/dev/null | head -10 || echo "Pods/ExpoModulesJSI not found"
          echo "=== ExpoModulesJSI in node_modules ==="
          ls node_modules/expo-modules-jsi/apple/Sources/ExpoModulesJSI/ 2>/dev/null | head -10 || echo "not found at that path"
          find ../node_modules -name "JSIRepresentable.swift" 2>/dev/null | head -5
          echo "=== All xcframeworks in Pods ==="
          find Pods -name "*.xcframework" -maxdepth 4 2>/dev/null | head -20

      - name: Build archive (unsigned)
        working-directory: mobile
        env:
          EXPO_PUBLIC_SUPABASE_URL: ${{ secrets.EXPO_PUBLIC_SUPABASE_URL }}
          EXPO_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY }}
          EXPO_PUBLIC_BUILD_ID: ci-${{ github.run_number }}
        run: |
          set -o pipefail
          xcodebuild archive \
            -workspace ios/LifeOS.xcworkspace \
            -scheme LifeOS \
            -configuration Release \
            -archivePath $RUNNER_TEMP/LifeOS.xcarchive \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            ENABLE_USER_SCRIPT_SANDBOXING=NO \
            SKIP_INSTALL=NO \
            CURRENT_PROJECT_VERSION=${{ github.run_number }} \
            -destination "generic/platform=iOS" \
            2>&1 | tee /tmp/xcodebuild.log | xcpretty; \
          STATUS=${PIPESTATUS[0]}; \
          if [ $STATUS -ne 0 ]; then \
            echo "=== XCODEBUILD ERRORS ===" && \
            grep -E "(error:|PhaseScript|FAILED|Script-)" /tmp/xcodebuild.log | head -80; \
            exit $STATUS; \
          fi

      - name: Package IPA
        run: |
          APP_PATH=$(find $RUNNER_TEMP/LifeOS.xcarchive/Products -name "*.app" | head -1)
          echo "App path: $APP_PATH"
          ls "$APP_PATH" | grep -E "Info.plist|_CodeSignature" || echo "WARNING: missing expected files"
          mkdir -p Payload
          cp -r "$APP_PATH" Payload/
          zip -r LifeOS-unsigned.ipa Payload/
          echo "IPA size: $(du -sh LifeOS-unsigned.ipa)"

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: LifeOS V2
          path: LifeOS-unsigned.ipa
          retention-days: 7
